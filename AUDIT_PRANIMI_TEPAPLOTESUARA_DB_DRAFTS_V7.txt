AUDIT — PRANIMI / TË PA PLOTËSUARA V7

Finding:
- Previous versions still had multiple physical draft traces: localStorage, Supabase Storage drafts/*.json, session, code reservations, and final public.orders.
- Supabase Storage contained many stale draft objects that matched final orders.
- Local-only save did not satisfy cross-worker visibility.

New shared source-of-truth:
- public.orders rows with status='incomplete' / data.status='incomplete'.

Remaining local storage role:
- Local storage remains only a safety fallback for this device/offline state.
- It is no longer the shared source-of-truth.

Storage role:
- New PRANIMI incomplete draft JSON objects are no longer written to tepiha-photos/drafts.
- Existing stale Storage objects remain untouched and are ignored by the list logic.

Code lifecycle:
- When a draft becomes meaningful, code is held with holdBaseCodeForDraft.
- The DB draft row stores code + local_oid + created_by_pin/lifecycle data.
- On final save, the same local_oid is upserted to a final active order and markCodeUsed still runs only after DB verify.
