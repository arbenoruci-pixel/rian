ROOT-CAUSE DIAGNOSTIC PATCH V1 — PRANIMI → PASTRIMI SAVE FAILURE / 732 TYPE CASE

Files changed:
- lib/orderSaveTrace.js
  Adds safe local trace logging to tepiha_order_save_trace_v1 with MAX_TRACE=300, save_attempt_id support, PA NUMËR helpers, safe payload summarization, DB verification by local_oid first, and sanitized local debug export.

- app/pranimi/page.jsx
  Adds save_attempt_id for each final save attempt; logs order_started, code_assigned, db_verify_success, db_verify_failed; normalizes PA NUMËR so client_phone is null; verifies DB after save using local_oid first and code only as fallback; shows LOCAL / NOT SYNCED with RETRY and EXPORT DEBUG when DB confirmation fails.

- lib/syncManager.js
  Logs local_order_saved and outbox_inserted when PRANIMI queues a base order, preserving save_attempt_id in local payload/data.

- lib/syncEngine.js
  Logs supabase_insert_attempt, supabase_insert_success, and supabase_insert_error during queued base order sync.

- app/pastrimi/page.jsx
  Adds diagnostic separation for unverified/problem cards, shows ⚠️ POROSI ME PROBLEM, blocks cache clearing while problems exist, keeps problem cards out of the normal confirmed list, and opens a PROBLEM ORDER diagnostic panel instead of silently failing.

Safety confirmations:
- No SQL.
- No DB schema change.
- No DB reset.
- No ARKA, payroll, company budget, or transport finance change.
- No release or reuse of code 732 or any burned/reserved code.
- No cleanup for code 732.
- No automatic cache clearing.
- No automatic Pastrimi repair.
- No automatic DB row creation from Pastrimi.
- No auto-delete of localStorage, IndexedDB, search cache, or outbox records.
- Trace/export sanitizes photos, base64, blobs, files, images, and attachments; long strings are truncated.

Validation performed:
- Direct JSX/esbuild syntax transform passed for app/pranimi/page.jsx and app/pastrimi/page.jsx.
- Direct esbuild syntax transform passed for lib/orderSaveTrace.js.
- node --check passed for lib/syncManager.js and lib/syncEngine.js.

Follow-up safety fix:
- app/pastrimi/page.jsx: updated isConfirmedDbPastrimRow(row) so base orders still require numeric ids, while transport_orders are confirmed with either numeric ids or UUID ids when a valid T-code/code identity exists.
- This prevents valid transport DB rows from being hidden or misclassified as problem cards only because their id is UUID.
