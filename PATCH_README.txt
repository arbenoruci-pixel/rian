// PATCH NOTE:
// Minimal DISPATCH fix:
// - Use fallback amount (cash_left ?? keep_cash ?? cash_counted)
// - Do NOT change any other logic or UI

// Apply this by replacing ONLY the DISPATCH map block in your CashClient.jsx
